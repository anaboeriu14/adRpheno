% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cognitive_composites.R
\name{create_adjusted_composites}
\alias{create_adjusted_composites}
\title{Compute Adjusted Composite Scores}
\usage{
create_adjusted_composites(
  dataf,
  test_groups,
  grouping_vars,
  filters = NULL,
  digits = 3,
  force = FALSE
)
}
\arguments{
\item{dataf}{A data frame containing the raw test scores and demographic information.}

\item{test_groups}{Named list of character vectors, where each element contains
column names for a test group (e.g., list(memory = c("test1", "test2"),
executive = c("test3", "test4"))).}

\item{grouping_vars}{Character vector of column names to group by for z-score calculation
(e.g., c("age_group", "education_group", "language")).}

\item{filters}{Named list of filtering conditions (optional).}

\item{digits}{Integer indicating the number of decimal places for rounding (default: 3).}

\item{force}{Logical, whether to force recalculation if already processed (default: FALSE).}
}
\value{
A data frame with the original data, adjusted z-scores, and computed composite scores.
}
\description{
Computes z-scores for test scores adjusted for demographic factors and
calculates composite scores for specified test groups.
}
\details{
This function expects that demographic grouping variables already exist in the data frame.
To create standardized demographic groupings, consider using the \code{create_flexible_groups()}
function from the adRutils package first.
}
\examples{
\dontrun{
# Define test groups
test_groups <- list(
  memory = c("LM1", "LM2", "SEVLT_imm", "SEVLT_del"),
  executive = c("digit_span", "trails_a", "trails_b"),
  verbal = c("FAS", "animal_naming")
)

# Calculate composite scores with demographic adjustment
result <- create_adjusted_composites(
  dataf = my_data,
  test_groups = test_groups,
  grouping_vars = c("age_group", "edu_group", "language_group")
)
}
}
\seealso{
\code{\link[adRutils]{bin_and_categorize_variables}}
}
