% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variable_merging.R
\name{merge_related_variables}
\alias{merge_related_variables}
\title{Merge Related Variables}
\usage{
merge_related_variables(
  dataf,
  var_groups = NULL,
  pattern_extract = NULL,
  pattern_replace = NULL,
  prefix = "",
  force = FALSE
)
}
\arguments{
\item{dataf}{A data frame}

\item{var_groups}{Named list. Groups of variables to combine, where each element
is a character vector of variables in priority order}

\item{pattern_extract}{Character. Optional regex pattern to extract group identifiers}

\item{pattern_replace}{Character. Optional regex pattern to determine output column names}

\item{prefix}{Character. Prefix for output column names (default: "")}

\item{force}{Logical. If TRUE, bypasses the check for previous processing (default: FALSE)}
}
\value{
Data frame with combined variables
}
\description{
Combines related variables into a single variable, using a priority order.
Useful for merging data collected at different time points or from different sources.
}
\examples{
\dontrun{
# Manual specification of variable groups
data <- data.frame(
  zscore_r3_nba = c(0.1, 0.2, NA),
  zscore_r5_nba = c(NA, 0.3, 0.4),
  zscore_r3_nhl = c(0.5, 0.6, NA),
  zscore_r5_nhl = c(NA, NA, 0.7)
)

# Method 1: Manually specify groups with priority
var_groups <- list(
  nba = c("zscore_r3_nba", "zscore_r5_nba"),
  nhl = c("zscore_r3_nhl", "zscore_r5_nhl")
)
merge_related_variables(data, var_groups, prefix = "zscore_")

# Method 2: Automatic grouping by pattern
merge_related_variables(data,
                       pattern_extract = "r\\\\d+",
                       pattern_replace = "zscore_r\\\\d+_",
                       prefix = "zscore_")
}
}
