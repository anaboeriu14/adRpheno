% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drug_pivot_wide.R
\name{pivot_medication_data_wide}
\alias{pivot_medication_data_wide}
\title{Pivot medication data from long to wide format}
\usage{
pivot_medication_data_wide(
  dataf,
  id_col,
  medication_col,
  position_col,
  category_cols = NULL,
  max_meds = NULL,
  fill_value = NA,
  med_prefix = "new_"
)
}
\arguments{
\item{dataf}{Long-format data frame.}

\item{id_col}{Participant ID column.}

\item{medication_col}{Medication name column.}

\item{position_col}{Column whose values encode the position of each
medication (e.g. \code{"medication_1_name"}, \code{"medication_2_name"}). The
leading integer is extracted to determine the order. \strong{Required;
no default.}}

\item{category_cols}{Optional binary indicator columns to aggregate. If
supplied, also produces \verb{total_*_meds} (count per participant) and
\verb{taking_*_med} (0/1 indicator) summary columns.}

\item{max_meds}{Maximum medications per participant (default: \code{NULL},
meaning all).}

\item{fill_value}{Value for missing medication slots (default: \code{NA}).}

\item{med_prefix}{Prefix for medication columns (default: \code{"new_"}).}
}
\value{
Wide-format data frame with one row per participant.
}
\description{
Transforms medication data from long format (one row per medication) to
wide format (one row per participant) while preserving medication
positions and aggregating category indicators.
}
\details{
Summary column names assume \code{category_cols} follow the \verb{is_<name>_med}
convention produced by \code{\link[=categorize_drugs]{categorize_drugs()}}. For input
\code{is_diabetes_med}, the function generates \code{total_diabetes_meds} and
\code{taking_diabetes_med}. Columns not matching this pattern still work but
produce summary names that retain the original prefixes/suffixes.
}
\examples{
\dontrun{
wide_df <- pivot_medication_data_wide(
  long_df,
  id_col         = "participant_id",
  medication_col = "med_name",
  position_col   = "med_slot",
  category_cols  = c("is_diabetes_med", "is_hypertension_med")
)
}
}
